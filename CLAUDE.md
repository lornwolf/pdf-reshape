# PDF 整形

扫描书籍 PDF 的整形工具：**纠正页面倾斜**，并把**版心（文字区域）移到页面正中**。
不做书脊弯曲（dewarp）之类的复杂修正——这是用户明确排除的范围，不要往这个方向扩展。

## 文件

| 文件 | 作用 |
|---|---|
| `pdf_reshape.py` | 核心处理 + 命令行。所有算法都在这里 |
| `pdf_reshape_gui.py` | tkinter 图形界面，只负责界面和线程，算法一律调用 `pdf_reshape`（导入名 `core`） |
| `pdf_reshape_store.py` | 校正进度的持久化（SQLite）。只被界面使用，命令行不读写数据库 |
| `PDF整形.bat` | 双击启动界面（`pythonw`）。内容保持纯 ASCII，避免 cmd 代码页问题 |
| `tests/` | 回归测试，见 `tests/README.md`。`run_all.py` 一键运行；`test_core.py` 核心、`test_gui.py` 界面（18 个场景）；`check_book.py` 用真实的书核对 |

依赖：Python 3.12、PyMuPDF (`fitz`)、OpenCV、NumPy。界面只用标准库 tkinter，**不要引入 PySide/Pillow 等额外依赖**（预览图用 `cv2.imencode` → base64 → `tk.PhotoImage`）。

## 运行与验证

```bash
python pdf_reshape.py 书.pdf [-o out.pdf] [--pages 1-20] [--skip-pages 3,8] [--delete-pages 1,2] [--cleanup-pages 4,7] [--enhance] [--clean-margin] [--upscale] [--min-angle 0.3] [--book manga] [--flatten]
python pdf_reshape_gui.py [书.pdf]

# 回归测试（不依赖 pytest）
python tests/run_all.py             # 全部，约 2 分钟；界面测试会弹出窗口自己操作
python tests/run_all.py --no-gui    # 只跑核心测试，约 1 分钟
python tests/test_gui.py marks      # 只跑名字里含 marks 的界面场景
python tests/check_book.py 书.pdf --pages 9,27     # 用真实的书核对（分析结果会缓存）
```

**改完代码先跑 `run_all.py`，改了版心检测或对齐规则还要跑 `check_book.py`。** 修 bug 时先写一个能复现它的测试。测试全部使用临时目录和临时数据库（`PDF_RESHAPE_DB`），不碰用户真实的进度库；界面测试每个场景一个子进程、带超时——界面测试一旦出错很容易卡在事件循环里不退出（卡过 10 分钟）。
测试曾经只是 scratchpad 里的一次性脚本，功能改了没人跟着改、也没法重跑；整理进仓库的当天就抓到一个没注意到的回归（合成测试第 5 页偏了 1%）。**新功能的验证脚本直接写进 `tests/`，不要再写成一次性的。**
临时文件放 scratchpad，不要放进项目目录。

改动版心检测或对齐规则后，**必须用真实扫描书整本验证**，合成数据覆盖不了真实的边缘杂质。做法：分析全书 → 列出版心边缘向外偏离同奇偶页中位数 >2.5% 的页 → 逐个看调试图。基准样本有三本（都在用户本机上，不入库；需要时向用户要路径）：
- 《日语最该掌握的句型840个》（397 页，1bit 黑白，约 178 DPI，扫描图居中放在 A4 页面里只占 83% 宽）：考验边缘黑线的清理。剩 11 页"异常"，都是封面封底和原扫描就把文字切到边的页，属于正确判定。原文件已经不在了，用户手上只有处理后的「（修正版）」——它是早期版本的输出，扫描边缘线已经被烤进页面内部，约 46 页的版心因此偏宽；这是该衍生文件特有的，**不要为它调参**。
- 《一个中国革命亲历者的私人记录》（282 页，1bit 黑白 287 DPI，页码外挂在正文外侧）：考验「对齐看正文」。期望标准正文宽 0.865，有无页码的页处理后正文左右边距都是 0.067～0.069。
- 《初级中学课本 语文 第二册》（人民教育出版社）（351 页，灰度 JPEG 整页扫描，左右页版心位置相差约 7%，前置部分奇偶对应关系与正文相反）：考验对齐规则。期望：全宽的页处理后左右边距差 <1%；16 页单侧缩进的页（9、50、56、95、212、230、244、272、278、300、302、304 靠右，314、320、327、350 靠左）有一边的边距等于标准边距 0.114；且有 6 页（1、27、141、171、207、260）被 `effective_boxes` 剔掉了空白处的杂质。

## 处理流程

两遍处理，核心 API 都接受 `progress` / `cancelled` 回调（界面用），取消时抛 `Cancelled`：

1. `analyze_document` → 每页一个 `PageInfo`（取图方式、倾斜角 `angle`、版心 `bbox`）。
2. `compute_reference` 统计全书标准版心 → `plan_page` 决定每页实际的旋转角和平移量 → `process_document` 做变换并写出。

`preview_page` 供界面预览，和实际输出走同一套 `plan_page` + `transform_image`，保证所见即所得（黑白页的预览也会二值化）。

所有坐标（`bbox`、`scan_rect`、平移量）都**按图像宽高归一化到 0~1**，因为各页像素尺寸可能不同。分析在长边 1200px 的缩略图上做，变换在原分辨率上做。

## 关键设计决定（改之前先读）

### 取图：优先内嵌原图，不渲染
页面只有一张正放的图时（`classify_page` → `native`），用 `fitz.Pixmap(doc, xref)` 取原图，不用 `get_pixmap` 渲染，否则 1bit 图会被放大成灰度 JPEG，又糊又大。
扫描图**不一定占满页面**：`place_on_page` 按它在页面中的位置贴到整页画布上（保持原图分辨率），并记下 `scan_rect`。只要覆盖页面 50% 以上就走 native。
没有图像的页、空白页、无需修正的页，用 `insert_pdf` 原样复制。
**内容占满整页**（版心宽高都超过 95%，`PageInfo.full_bleed`）：分析时只做标记、结果照存，`plan_page` 按书的类型决定——文字书当整页图片原样保留（和彩色封面一样，指定了去污/高清化则位置不动只做那件事），**漫画照常纠偏、找平、高清化**（出血的画面很常见，《スラムダンク 第27巻》193 页里 79 页；早先一律记成「原样复制」，这些页既不纠偏也不能高清化，用户报的）。两种书里都**不参与标准版心的统计**（`compute_reference` 的 `stat`），否则漫画的标准版心就不是分格框的大小了。这种页没有文字行也没有框，投影法只能靠排线、网点，常常错得离谱（三卷《スラムダンク》188 页出血页里，置信度不到 5 的二十几页被测成 1°～5°，都是正的页；置信度 5 以上的全在 1.2° 以内）：置信度 <5（`FULL_BLEED_MIN_CONF`）就不旋转。
**彩色封面**（`is_full_bleed_color`，`PageInfo.full_bleed_color`）：整页图片本来靠「深色内容占满 95%」来认，封面上浅色的渐变底不算深色内容，封面因此被当成普通页——标题加插画那一块成了版心、拿去和文字页对齐，出血的封面被平移 5%，一边露出一条平色带。所以分析时另记一个标记：带颜色（彩度 >20 的像素占 5% 以上）且四周 3% 的边缘带里够亮的像素不到一半。**只在文字书里生效**（`plan_page` 按 `opts.book` 判断，分析结果与类型无关、换类型不用重新分析）：漫画整页都是画面，出血的彩页也要照常纠偏、对齐。带白边的彩色封面照常处理，需要的话手动「本页不纠偏居中」。指定了去污的封面和「本页不纠偏居中」一样：位置不动、只去污。
**图像蒙版（ImageMask）**：有的扫描软件把黑白页存成没有色彩空间的 1bit 蒙版，`fitz.Pixmap(doc, xref)` 得到 `colorspace is None`，对它做色彩空间转换会报 `source colorspace must not be None`（用户遇到过）。蒙版只记录哪些点上色，颜色和 0/1 的含义由页面决定，不能直接当灰度图用；`load_native_image` 对这种图改为**按图自身的分辨率渲染它所在的区域**（像素一一对应），再二值化去掉抗锯齿。测试文件用 scratchpad 里的做法生成：插入 1bit PNG 后 `xref_set_key(x, "ImageMask", "true")` 并把 `ColorSpace` 设为 `null`。
**单页出错不能让整本书失败**：`analyze_document` 里某一页抛异常，就把它记成 `copy`（原样保留）并在 `note` 里写明原因，其余页照常。真实的扫描 PDF 千奇百怪，总会有没见过的结构。

### 版心检测：`make_ink_mask` 的四步清理
真实扫描件的版心被框大，几乎都是边缘杂质造成的，不是正文里的污渍：
1. 贴着**扫描图边缘**（不是页面边缘！用 `scan_rect`）3px 内的连通成分。
2. 细长线（膨胀后按 `minAreaRect` 判粗细/长度）。标题装饰线也会被去掉，无妨——它在文字范围之内。
3. 整个落在扫描图左右边缘 3% 地带内的任何东西；上下边缘地带只去细线和小块（那里可能有页眉页码）。
4. 周围没有其他内容的孤立小污点。

贴边判断**不能在膨胀后的图上做**：扫描裁得很紧时，整行文字膨胀后会连成一块并碰到边缘，被整行删掉。
页码、页码装饰（样本书每页底部的黑色椭圆）属于版心，要保留。

### 对照全书剔除空白处的杂质：`effective_boxes`
空白处的小污渍（《语文第二册》第 27 页右侧一段 1×14 像素的竖线）会把版心撑大：框仍然居中，文字却偏到一边。它离扫描边缘超过 3%，`make_ink_mask` 的四步都滤不掉；而**单看一页分不清它是污渍还是页码**（个位数的页码也就这么大），放宽"小污点"的尺寸会误删页码。
- `detect_content_bbox` 每页记三样：全部内容的版心 `bbox`、只算成块内容的 `core_bbox`（膨胀后长 >3% **且**粗 >1.2% 页面长边；只长不粗的是线段）、伸出核心之外的小块 `outliers`。
- `effective_boxes` 对照全书标准版心：某个方向的版心比标准大 2.5% 以上（`OVERSIZE_TOLERANCE`）才动手，由外向内逐个剥掉小块，两侧都有时先剥「离里面的内容更远」的一侧（污渍孤零零的，页码、目录序号紧挨正文），剥到大小正常为止。带页码的版心本来就和标准一样大，不会被动到。
- 剥离的结果是 `ref["content"]`（全部内容、不含杂质）；对齐用的 `ref["boxes"]` 在此基础上还可能按正文范围再收窄，见下一节。
- 不能只在"全部 / 核心"两档里二选一：目录页的序号列全是小块，整列换成核心版心会窄 3%（试过）。
- 标准版心统计两遍：先用原始版心估，剔完杂质再统计一遍。一律通过 `page_box` / `content_box` 取版心，**不要直接用 `info.bbox`**。

### 对齐要看正文，不是页码：`dense_extent`
《一个中国革命亲历者的私人记录》把页码（带一块实心灰色花饰）挂在正文**外侧的空白里**，左页在左下角、右页在右下角，右页的页眉也伸到同一侧。带页码的版心（0.92）比正文（0.865）宽一截：拿它居中，正文就一页偏左一页偏右各 2.7%；没有页码的章首页还被当成「略窄」贴到了一边（用户报的就是这个）。
- `dense_extent` 数「每一列被多少行文字覆盖」：按行投影切出一行一行（**按墨迹的多少切**，不能只看有没有墨迹——页边一串污点会让整页连成一行），正文的列被几十行覆盖，页码、页眉各占一行。门槛 `max(3, 8% × 行数)`：页眉和页码常挂在同一侧，所以至少 3 行。边缘精修也只认「至少被 2 行盖住」的列（花饰可能就贴在正文旁边不到一个字宽）。
- **不能比墨迹的多少**（试过）：实心的花饰那几列墨迹不比正文少。
- 只适合**沿文字行的方向**：行堆叠的方向上段落末行可能只有两三个字，和页码分不开。两个方向各求各存（`dense_bbox`），横排书的竖直方向本来就求不出来，要回退到版心本身，**不能因此把水平方向的结果也丢掉**（犯过）。哪个方向是文字行方向，到 `compute_reference` 才按全书多数决定。
- **只在全部内容比标准正文明显宽的时候启用**（`content 宽 > 标准 + 3%`，`HANGING_TOLERANCE`）。门槛曾经是 2%：「标准」是按行覆盖求的，行尾参差不齐的书里它本来就比实际内容窄一点，2% 会误伤正常的页（合成测试第 5 页因此偏了 1%）；外挂的页码至少伸出去 5%，3% 两头都够。内容不超宽的页直接用全部内容：整行文字只有寥寥几行的页（诗歌、字表、目录）按行覆盖求出来会偏窄。《语文第二册》350 页只有 4 页启用，其余与之前逐页一致；《亲历者》274 页里 207 页启用。
- 于是每页有两个版心：`page_box`（对齐用，`ref["boxes"]`）和 `content_box`（全部内容，`ref["content"]`）。**全局的「版心外去污染」（`--clean-margin`）必须用 `content_box`**，否则会把挂在外面的页码涂掉。

### 倾斜检测：投影轮廓法 + 漫画分格框把关
`detect_skew` 同时算行方向和列方向的投影得分，取峰值更尖锐的一方，所以横排竖排通用、无需用户指定。粗搜 0.5° + 细搜 0.05°。搜到的角度就是直接施加给 `warpAffine` 的角度，不存在正负号换算（正角度 = 顺时针歪的页转回来；横线向右下降、竖线向下往左偏的页要正角度）。
**漫画会把投影法带偏**（《スラムダンク 第27巻》第 13 页被转了 -3.95°，第 8 页 -2.40°，用户报的）：斜着的分格线、格子里的密排线、网点的点阵（行列两个方向同时出尖峰，第 23 巻第 15 页 0.9° 处的峰比框尖 10 倍）在投影上都比矩形的分格框尖。而且**分格框贴着扫描图的边时，`make_ink_mask` 第 1 步把贴边的成分整个去掉，框连同和它相连的画就没了**，剩下的只有排线。所以另备一份含框的墨迹 `frame_ink`（只二值化；贴边的**粗**块——扫描黑边——连同周围抹掉，细线留下；最外 3 像素抹掉，画布的边本身是直线），用 `frame_scores` 把关：把轮廓转到每个粗搜角度，用一维开运算数够长（长边 10%）的水平直段和竖直直段，**取两者中小的**——矩形的框两个方向都有，斜分格线只有一个方向。在缩小一半的图上做（每页多 0.06 秒）。峰值 ≥0.6 倍长边就算有框，这时 `refine_frame_angle` 在峰的角度附近把够长的直段各自拟合成直线、取加权中位角（一行粗的段是斜线被切下来的台阶，看不出角度，要跳过；粗搜的直边总长是个平顶、分不清 1° 以内，允许修正 ±1°），**和投影法差 ≥0.5° 就改用框的角度**；一条也拟合不出来、或者**横线和竖线各自的角度差太大**（不是刚性转过的矩形——文字书里的表格因为纸面有弧度就是这样，《语文第二册》第 124 页差点被改了 0.5°，这时文字行才是准的；门槛 `FRAME_SQUARE_TOLERANCE` 0.25°，但投影法错了 1° 以上时放宽到 0.6°——第 13 页的框横竖就差 0.29°，错得离谱时框只要大致方正就该信）就仍按投影法。粗搜格子上的直边总长**不能拿来比谁高**（平顶；曾这样比，《第27巻》第 59 页框在 0°、投影法说 1.1° 没比出来）；细找也**不能再用投影**（会往错的峰上靠：《第23巻》第 15 页投影法说 0.85°，框在 0.6°，细找限制在 ±0.5° 里就停在 0.5° 的边上）。文字书没有框，不受影响（合成测试的文字页角度完全不变；三本真书 1028 页整本对比 0 页变化）。曾试过「旋转后包围盒最紧」（内容贴边的页转一转四角被切掉、包围盒反而更小，全错）和 Hough 直线（分格线和框的长度相当，分不出）。

### 对齐规则：文字书一律居中，漫画走 `axis_shift`
**文字书**（`compute_shift(book="text")`）：每一页把自动检测的版心**居中**，没有别的规则（用户要求「默认统一居中」）。半页、缩进页、被切掉的页由用户处理：分析完后版心比标准窄或矮 4% 以上的页（`notable_pages`，`NOTABLE_SHORTFALL`）在界面的翻页滚动条上标黄，翻到时出现「确认完毕」；「版心：靠左/靠上/居中/靠右/靠下」按那一刻的红框算出平移量记在 `ref["shift"]`（`pages.shift`），「手动调整」用方向键、鼠标拖动改它。**调红框本身不移动页面**（用户要求，早先每调一下页面就跟着重新对齐一次）：默认的平移量按不含微调的自动版心算（`align_box`），按了对齐按钮之后才按红框算、并且固定下来。「靠边」贴的是全书标准版心居中放置时的边（`standard_edges`）。
以前有一套按全书统计对齐的规则（整页居中、略窄的页看窄在哪一边、半页贴齐匹配的那条边、都对不上按平均偏移），一侧缩进的规则在《语文第六册》第 335 页上误判（几何上分不清「缩进」和「只是整块窄一点」），用户要求删掉规则、统一居中。`axis_shift` 里剩下的规则**只给漫画用**：
1. 版心尺寸与标准版心相差 2% 以内（`FULL_PAGE_TOLERANCE`）→ 单独居中，精确抵消该页的扫描偏移。
2. 沿文字行的方向只是略窄（12% 以内，`NARROW_TOLERANCE`）→ 居中。
3. 某一边与标准版心边缘的中位数位置相差 3% 以内（`EDGE_MATCH_TOLERANCE`）→ 把这一边贴齐标准版心（章末页贴上边、无页眉页贴下边）。**先比本页所属的奇偶类，对不上再比另一类**。
4. 都对不上（前言、落款等特殊版式）→ 略小的单独居中；小很多的只按全书平均偏移移动（取两类的平均）。

**页序的奇偶不可靠**：前置部分和正文之间多一页或少一页，左右页的对应关系就反了（《语文第二册》第 9 页目录因此曾被往反方向推了 2.8%）。任何用到奇偶类参照的地方都要考虑这一点。

排版方向取全书多数页的判定（`ref["vertical"]`，来自 `detect_skew` 选中的投影方向），不看单页——单页约有 10% 会误判。

标准版心按奇偶页分开统计边缘位置（订口/切口不同），尺寸用全书中位数。界面里只处理部分页时，标准版心仍取全书的（`process_document(ref=...)`）。

### 画质
- 旋转 + 平移合成一个仿射矩阵，只重采样一次。
- **不旋转的页只做整像素平移 + `INTER_NEAREST`，完全无损**。不要改回小数平移。
- **原图是 JPEG 时，不旋转的页的平移量要凑成编码块（灰度 8、彩色 16 像素）的整数倍**，并且要算上原图在整页画布里的偏移（`PageInfo.grid`），让原图和输出的块网格重合。实测重存后 PSNR 从 34 dB 提高到 42 dB、体积从原图的 1.5 倍降到 1.15 倍；代价是居中精度变成半个块（约页宽的 0.3%）。`jpeg_block` / `grid` 由 `load_page_image` 每次填写，不依赖保存下来的分析结果，所以加这两个字段没有动 `ANALYSIS_VERSION`。
- JPEG 编码开了 `IMWRITE_JPEG_OPTIMIZE`（画质不变，小 5～10%）。
- 黑白二值页旋转后要重新二值化，存成 1bit PNG（`IMWRITE_PNG_BILEVEL`），笔画边缘会略糙；`--upscale` 以 2 倍分辨率输出来缓解，体积约 2.4～2.7 倍。

### 书的类型：`Options.book`、`BOOK_OPTIONS`、`BOOK_DEFAULTS`
用户要求界面上有「文字书 / 漫画书」下拉框，两类书显示不同的「修正内容」选项、分析完给不同的建议。**漫画默认不勾「版心居中」**（用户定的）：和 `Options` 默认值不同的选项登记在 `BOOK_DEFAULTS`，`Options.for_book(book)` 取这类书的默认；界面只在用户**在下拉框里换类型**时（`select_book_type`）按它改勾选，打开有记录的书恢复的是保存的设置、不能被盖掉；命令行 `--book manga` 也按它来，`--center` / `--no-center` 显式指定。哪类书显示哪些选项登记在 `BOOK_OPTIONS`；界面按它 `pack`/`pack_forget` 勾选框（`apply_book_type`），**藏起来的勾选框可能还勾着**，所以 `Options.effective()` 按类型把不适用的选项统一关掉——界面的 `get_options()`、核心的 `process_document` / `estimate_output_size` / `preview_page` 都过一遍它，核心函数不用各自判断。类型存在书的选项 JSON 里（`OPTION_VARS` 含 `book`），换类型不用重新分析（分析结果与类型无关：黑白页的增强评估和灰度页的灰影都量好了，只是各取所需）。建议值现在有开关类的（`flatten` 是 True/False），`REC_ROWS` 里的比较和显示用 `rec_same` / `rec_text`；`format_recommendation(book=...)` 的 `lines` 已含「显示增强」（文字书）或「纸面找平」（漫画）的那一句，界面不要再另外打印。

### 纸面找平（漫画）：`flatten_paper`、`measure_shade`
漫画扫描件的通病是**订口阴影**：页宽 5%～10% 的一道灰影、二三十级、每页位置固定，看着灰蒙蒙；纸面本身也常不是纯白（样本《スラムダンク 第23巻》中部 246～249、页边 222～225）。用 `paper_map` 估计每一处的纸色，按「当地纸色 → 白」**乘**一个系数：灰影没了、纸变白，墨线和网点相对纸面的深浅不变。**不二值化、不锐化、不去噪点**——漫画的网点经不起，任何重采样都会出摩尔纹。
- **纸色不能用去污那套闭运算（`paper_tone`）估**：它先把图缩小 4 倍，网点一缩就糊成一片浅灰，闭运算看不到点与点之间的白，整片天空被当成发灰的纸面乘了 1.24，网点拉淡、拉出格子（用户看《スラムダンク》第 9 页的天空报的）。`paper_map` 在**原分辨率**上把页面分成 64×48 格，每格取最亮 5% 像素的亮度：文字、排线、网点占不满一格，最亮的 5% 总是点与点之间的白缝；整格实心黑（不够 `PAPER_MIN` 160）由周围的格子插值。格子（约页宽 2%）比订口阴影窄得多，页边那一格估的才是页边的纸色（4% 的格子估的是它靠里的一侧，页边找不平）。
- 实心黑的地方按周围的纸色算系数，乘上去仍然是黑；中等的灰和周围一样略微拉亮，不会出接缝。
- **开了就每一页灰度页都做**，不按灰影逐页取舍：找平没有体积的代价，漫画要的是页与页一致（只做有灰影的页，翻到没灰影的页纸色就从白跳回灰）。每页的灰影 `PageInfo.shade`（`measure_shade`：四条边各取 8% 宽的带子和中部比）只用来给建议：≥12 级（`FLATTEN_MIN_SHADE`）的页占一成以上就建议开启。真书 19 页抽样里 16 页 ≥12。
- 在变换之前对原图做（旋转补的边就是白的）；「本页不纠偏居中」的页不做。合成样本 `manga.pdf` 的排线和实心黑块用来验证墨迹不变。
### 显示增强：`assess_enhancement`（「显示增强」勾选项 / `--enhance`）
用户的要求：**不是对所有页，只对能有明显改善的页**，自动选方法；只用三种经典方法，明确不要同形字符归并（JBIG2 式，有认错字的风险）和 AI 超分（会"猜"出清晰的错字）。只针对黑白二值页，灰度/彩色页不处理。每页在分析时评估一次，结果存在 `PageInfo.enhance`（如 `"smooth3+clean"`）：
- `smooth2/3`（放大 2/3 倍、轻微模糊、重新二值化）：只给 **<240 DPI** 的文字页（<200 DPI 用 3 倍）。分辨率够的页正常阅读时看不出区别，文件却要大几倍——《亲历者》287 DPI 全书一页都不选。
- `clean`（`find_noise`）：去噪点、填小孔，**条件极严**：噪点必须孤立（周围一圈内没有别的墨迹——紧挨着字的小点可能是浊点、标点）；小孔必须被厚墨迹四面包住（细笔画围起来的空白是字形里的圈，「あ」「ぬ」）。**不做「接断笔」**。杂质 ≥120 个且 ≥ 字数的 8% 才算值得。
- `vector`（`vector_stream`）：轮廓转贝塞尔曲线写成 PDF 路径，只给以大字/线条为主的页（大块占墨迹 ≥60% 且成分 <600）。小字矢量化会"融化"，数据量也太大。
- **网点图案必须让开**（`pattern_regions`：小点、小孔密集成片的区域）。实测真实扫描书里绝大多数"小孔"都在抖动出来的页码花饰、章节图标里，不是笔画缺损；最初没有这层保护时《亲历者》173 页里 171 页被判为"值得去噪点"。图案区域：不填孔、不去点、不平滑，放大时用最近邻（三次插值会把规则的网点拉成斜纹）。图案占页面 10% 以上的页整页不增强。
- 「本页不纠偏居中」的页不增强。要增强的页不能原样复制（`plan_page`）；大小预估按「是否旋转 × 增强方法」分组抽样。
- 界面预览缩到窗口大小时增强的效果完全看不出来，所以有**放大镜**：在预览图上按住鼠标，两边同时放大同一处（`show_loupe`，用的是原尺寸的图 `preview_full`）。

### 高清化（逐页）：`super_resolve`、`sr_of`、`pages.sr`
用户先前明确不要 AI 超分做成全书的增强（会「猜」出清晰的错字），后来要求做成**逐页手动指定**的「高清化」按钮，先看效果。用外部程序 `realesrgan-ncnn-vulkan`（独立可执行文件，自带 Vulkan 推理，不需要 Python 依赖——不违反「界面不引入额外依赖」）：`find_sr_exe` 依次找 `PDF_RESHAPE_SR` 环境变量、`~/.pdf_reshape/realesrgan/`、程序目录下的 `realesrgan/`、PATH；找不到时按钮弹提示、不打开开关（`SR_DOWNLOAD`）。程序**不入库**，用户机器上在 `~/.pdf_reshape/realesrgan/`。
- 模型定为 `realesrgan-x4plus-anime`、4 倍：三个自带模型里在漫画（网点化成平色、线条最干净）和文字页（笔画圆润）上都最好；`realesr-animevideov3` 会把网点搓成蠕虫状的纹理，`realesrgan-x4plus` 效果略差还慢 4 倍。MX150 上漫画一页（737×1200）约 1 分钟，2400×1700 的文字页要 5 分钟（两个进程同时跑会翻倍）。**注意用户机器上 Vulkan 只看得到 Intel UHD 630**（三个设备都报成它，`-g 0/1/2` 时间一样），MX150 没有被用上——是 NVIDIA 驱动的 Vulkan 层没装好，不是程序的问题。进度：程序每算完一块往 stderr 打一行百分比，`super_resolve(progress=)` 据此报 0～1；预览时借用进度条、状态栏写百分比、完成后状态栏前缀「高清化完成 ✓」并写日志；处理时进度报成 `k - 1 + 进度`，落在这一页的格子里。
- 流程：先按平常那样变换（不放大、不增强，AI 取代它们），再放大 4 倍；黑白页直接以 4 倍分辨率二值化（仍是 1bit）、灰度/彩色页缩回 2 倍输出。`SR_CACHE` 留最近几页的结果：预览算过的处理时直接用，一页一分钟不能白算。对不纠偏居中的页、彩色整页图片也做（位置不动）。
- **大小预估不跑 AI**（每次改设置都要抽样，一页一分钟受不了）：高清化的页按经验倍数估（黑白 4 倍、灰度 3 倍）。测试里用三次插值冒充 `super_resolve`，只测接线；程序在的话另外拿一小块真的跑一次。
### 建议值：`format_recommendation`
分析完成后给四个参数提建议。`source_profile` 抽查十几页原图的压缩方式（只读文件头），`jpeg_quality_of` 从亮度量化表反推相当于 libjpeg 的哪一档质量。
- 「小于此角度不旋转」：黑白 <250 DPI → 0.3°，黑白 ≥250 DPI → 0.2°，JPEG 扫描 → 0.2°（不旋转的页能块对齐、几乎无损地重存），无损压缩的灰度/彩色 → 0.1°。
- 「JPEG 质量」：原文件是 JPEG → 原图质量 +5 后向上取整到 5 的倍数（62 → 70），限制在 50～95。**原图质量就是画质的上限**：《语文第二册》原图约 62，用 90 重存是 143 MB（原文件 55 MB），用建议的 70 是 71 MB。原文件是无损压缩 → 90。黑白书输出 1bit PNG，此项不起作用，只给出说明、不给建议值。
- 「倾斜检测范围」：有页面的倾斜顶在 ±max_angle 的边上（差 0.26° 以内）→ 建议放宽 3°（上限 15°），因为这些页实际可能更歪、被范围截住了（实测 6.5° 的页在 ±5° 下只测出 5.5°）；否则建议值就是当前分析用的范围——**不建议缩小**，缩小只是分析快一点，却要为此重新分析一遍。
- 「渲染 DPI」：只对取不了内嵌原图、需要渲染的页起作用。没有这种页 → 不给建议值，只说明此项不起作用；有 → 按这些页里图像本身的分辨率（取整到 50）。
- 这两项是「安静」的：建议值 = 当前值 = 默认值时不显示提示（绝大多数书都是这样），免得参数框里常年挂着四行蓝字。它们属于分析参数，界面上采用/恢复之后如果 `analysis_key` 变了，会立刻自动 `reanalyze()`，不用等到点「开始处理」。
- 命令行未指定 `--min-angle` / `--quality` 时自动采用建议值（倾斜检测范围和渲染 DPI 只打印建议，不自动采用——那需要重新分析）；界面只提示，**不自动改用户的设置**；所有建议值共用**一个**按钮（用户明确不要每项一个）：只要有一项还不是建议值就叫「采用建议值」、一次全部采用，全部都是建议值时改名「恢复默认值」、一次全部恢复成 `Options()` 的默认值（建议值恰好全等于默认值时不显示按钮）。以后再加别的建议值，登记到 `REC_ROWS` 并让 `format_recommendation` 返回对应的键即可。提示文字和按钮都要跟着当前设置值实时更新（`update_recommend_label`），用户手动改数值也算。

### 输出大小预估：`estimate_output_size`
- 原样复制的页按原 PDF 里图像流的长度算（没压缩过的流按压缩之后的大小算——输出保存时会顺带压缩它）；要重新编码的页均匀抽 12 页真的处理一遍，用「处理后 / 原图」的**比例**推算其余的页（页与页内容多少差别很大，按平均每页大小推不准）。
- **旋转的页和只平移的页分开抽样、分开算比例**（每组至少 4 页）：块对齐平移的 JPEG 页约 1.15 倍，旋转的页约 1.5 倍，混在一起抽样误差会到 +6%。
- 抽样页的大小**不能用编码出来的图像字节数**：PNG 交给 PyMuPDF 后会被重新压缩成 PDF 自己的流，1bit 页因此小 25% 左右（曾导致高估 34%）。现在是把抽样页写进内存里的临时 PDF、按正式输出相同的参数保存来量。
- 实测误差 ±3% 以内，用时 1～6 秒。界面上设置一变就自动重算（防抖 800ms，翻页不触发）；**调版心边框（箭头、与邻页相同、复位）时改为停手 2 秒之后才算**，并且点的瞬间就把排队中的、正在算的都停掉（`hold_estimate`）——用户连着点箭头时，每点一下算一遍把程序拖慢了，处理完会在日志里写实际大小供对照。
- 灰度 JPEG 扫描件重新编码后会明显变大（《语文第二册》55 MB → 质量 90 时 143 MB、质量 60 时 78 MB）：旋转后的图像比原始 JPEG 难压缩，这是重编码的固有代价，不是 bug。

### 手动调整与进度保存（`pdf_reshape_store.py`）
- 「本页不纠偏居中 / 恢复纠偏居中」（早先叫「本页不修正」，用户觉得容易误解成什么都不做——它只管位置）（普通按钮，文字随当前页的状态切换，和「删除当前页 / 恢复当前页」同一套做法）：`plan_page(skip=True)` 让该页不旋转、不平移，原样复制。**它管的是位置，不挡「去除边缘污染」**（曾经两者同时指定时去污不起作用，用户报的）：同时指定了去污的页不再原样复制，而是位置不动（整像素、无插值）只去污；全局的「版心外去污染」对不纠偏居中的页不生效。不动的页预览里也画红框——去污按红框算，而这类页的红框往往不准，得让用户先看到。被跳过的页的分析结果**仍然参与**全书标准版心的统计。
- 「删除当前页 / 恢复当前页」：`process_document(delete_pages=...)` 不输出这些页（原文件不动），目录书签的页码由 `remap_toc` 换算——指向被删页的书签改指后面第一个保留页，书签本身不删，免得破坏层级。被删的页同样仍参与标准版心的统计：标记一页不应该让别的页的对齐结果跟着变。删除优先于不纠偏居中。
- 「版心边框」微调（下拉框选上/下/左/右 + 箭头按钮，按住连续调，每步 0.2%；箭头是 ttk 按钮，和别的按钮一个样式，按住连续调靠 `start_repeat` 自己计时——ttk 没有 `repeatdelay`，连续调过之后松手要拦下按钮自己的 command，否则多走一步。第二排的按钮分三组用边框框起来：箭头 + 复位、与前后页相同、去污 + 梯形校正）：用户手动移动当前页红框的某条边，存在 `pages.box_adjust`（JSON `[左, 上, 右, 下]` 的移动量）。界面把它挂在 `ref["adjust"]` 上，`page_box` 自动加到对应的页——所以核心函数的签名都不用改，预览、处理、大小预估走的是同一条路。**只影响这一页**，不参与标准版心的统计。它只改红框（`page_box`），不改 `content_box`（涂白页边的范围）；页面按不按它对齐见下一条。每次重新 `compute_reference` 之后要 `attach_adjust()` 重新挂上。
- **版心的对齐按钮**（「版心移动：靠左/靠上/居中/靠右/靠下」+「手动调整」，只给文字书；最大化时放第二排、紧挨在「与后页相同」右边，窗口小时挪到第三排）：按那一刻的红框（含微调）算出平移量存成 `pages.shift`，从此这一页就用它；靠边只动那一个方向。「手动调整」是一个模式：方向键每按一步移动 0.2%（`NUDGE_STEP`），在「处理后」一侧**按在红框里拖动红框**（用户要求拖的是红框不是纸张；拖的时候只挪红框，松手后页面按新位置重排），翻页自动结束。「确认完毕」（绿）「下一处」「上一处」「撤销修正」（红）也在这一组里，一直显示、按状态灰掉（「下一处 / 上一处」翻到下一个、上一个还没确认的黄标页，到头了从另一头接着找，除当前页外没有别的黄标页时灰掉）：`lbl_nudge` 只放操作提示，不显示「本页已微调」「版心位置已指定」之类的文字（用户不要；有没有改过看「撤销修正」亮不亮）；撤销清掉微调、指定的位置并退回未确认。这一组的父级是 `right`，`relayout_align` 按第二排剩余的宽度用 `pack(in_=…)` 把它放到第二排（最大化时放得下）或第三排（窗口小），Tk 不能改父级、只能这样挪。窗口的位置和大小（含最大化）关闭时存进 `settings` 表（`save_window`），下次从同一个屏幕打开（多屏幕；那个位置已经不在任何屏幕上就不用）。「复位」把红框微调和指定的平移量一起清掉。早先是一个「版心居中」按钮加 `pages.box_align` 快照，已换掉（列留着不用）。「处理后」一侧还标注四条边到纸边的距离（`draw_margins`：只显示毫米，按 PDF 页面尺寸换算，字号 8；左右的数字在辅助线上侧，上下的在右侧）。两个预览之间是自己画的翻页滚动条（`draw_scrollbar`）：值得看一眼的页（`notable_pages`）标黄，点过「确认完毕」（`pages.confirmed`）的不再标。
- **用鼠标拖红框**：「处理后」一侧的红框在四角和四边中点各有一个白色小方块（`HANDLES`），拖角动相邻的两条边，拖边的中点只平移那条边。结果和箭头微调一样存成 `box_adjust`（拖过的边按拖动量算，没拖的边保持原值），所以复位、保存、预估都不用另写。按在小方块之外仍然是放大镜。只有画面上的红框就是当前页、当前设置下的那一个时才能拖（`box_editable`：翻了页或改了设置、新预览还没出来的那一小会儿不行，因为拖动量是相对画面上的红框算的）；分析、处理期间锁住，关掉辅助线时没有红框也就不能拖。拖动途中到达的预览结果直接丢弃。
- **梯形校正**（`pages.keystone`，`core.keystone_of` / `keystone_image` / `suggest_keystone`）：扫成梯形（透视变形）的页，用户要求的流程是——按「梯形校正」进入编辑，**「处理前」一侧**画出红色四边形（角点是原图坐标，所以画在左边；右边实时显示按它校正的结果），四个角可拖；按钮变成「执行校正」，按下后四边形围成的区域用 `warpPerspective` **铺满整页**（用户明确要求平铺，不留边距；要边距就把角往外拖）；校正过的页旁边有「取消校正」恢复原样，编辑中它是「放弃编辑」，翻页也放弃。初值 `suggest_keystone` 是墨迹凸包近似成的四边形：上面两个角靠满行的第一行、提得准，下面两个角只能靠最长的行和短短的末行，会偏，留给用户拖。校正过的页不再旋转、平移、去污（版心外的东西已经在页面之外），优先于「本页不纠偏居中」，预览里没有红框；「原样复制」的页（铺满整页的封面、空白页）也能校正——封面同样会扫成梯形，这类页按渲染 DPI 渲染后再校正（曾对它们禁用按钮，用户在《高中语文第 1 册》的封面上发现了）；显示增强照做，放大倍数和 `render_page` 里的规则一致。大小预估时归到「旋转」那一组（同样要完整重采样）。这和被排除的书脊弯曲（dewarp）不是一回事：透视是线性的，一次变换就拉直。
- 「与前页相同 / 与后页相同」（`box_like_neighbor`）：把当前页的红框设成和邻页一样，结果就存成上面的 `box_adjust`（可以再用箭头调、可以复位）。不能照搬坐标——左右页的版心在扫描图上差几个百分点（《语文第二册》7%），每页的扫描位置还有抖动，竖直方向能到好几个百分点。做法：取邻页红框的**大小**；「不平移 / 平移一个左右页位置差 / 反向平移」三种都试（页序的奇偶不可靠），取和本页检测到的边最吻合的；本页检测到的红框和邻页一样大时位置可信，直接对准，否则有一条边差 3% 以内就贴齐那条边。验证标准：正常页照搬正常邻页，红框变动的中位数应在 0.3% 左右、最大不超过 3%。
- 「去除边缘污染 / 取消去除污染」（`pages.cleanup`，`remove_margin_stains`）：逐页的开关，把**红框外**疑似墨迹的地方按周围干净的纸面重新画上。黑白页框外直接涂白；灰度/彩色页用大核闭运算估计「没有墨迹的纸面」（缩小 4 倍算；核取页面短边的 12%——核的半径要明显大于黑边的宽度，最靠边的像素才够得着里面的纸色，6% 的核在没旋转的页上会剩下一两列黑边），比纸面深 24 以上的像素换成**当地的**纸色。全局选项「版心外去污染」（`clean_margin`，`margin_clean_of`）做的是同一件事，区别是：全书生效、按 `content_box` 算（外挂的页码保得住）、**只对黑白页**（用户定的；灰度/彩色页要去污只能逐页点按钮）——1bit 的，加上存成 4bit/8bit 但实际上是黑白的（`PageInfo.mono`，`is_mono`：纸是白的、中间调不到 8%；《契诃夫短篇小说选》239 页 1bit 之外有 8 页 4bit，勾了去污页边的扫描线还在，用户报的），只在文字书里显示。它早先叫「版心外涂成纸色」，把版心外整块涂成全页的中位色，灰度页上会留下一道平色带。注意它按红框（`page_box`）算，挂在正文外侧的页码也在框外、会被去掉——用户需要保留页码时，应先用箭头把红框扩到包含页码。和 `adjust` 一样挂在 `ref["cleanup"]` 上，`plan_page` 里它会让本来「无需修正」的页也重新编码。
- 数据库默认在 `~/.pdf_reshape/books.db`。`books` 每本书一行（选项 JSON、输出路径、处理范围、当前页、分析参数、处理时间），`pages` 每页一行（`skip`、`deleted`、`cleanup` 三个开关、`box_adjust` 版心微调 + 该页 `PageInfo` 的 JSON）。以后要加别的逐页手动调整（手动角度、手动平移等），加在 `pages` 表上：开关类的登记到 `Store.PAGE_FLAGS`，通过 `set_page_flag` / `flagged_pages` 读写；存 JSON 的登记到 `Store.JSON_COLUMNS`，通过 `set_page_json` / `page_jsons` 读写；**新增列必须同时写进 `SCHEMA` 和 `Store._migrate`**（用户机器上已有旧结构的数据库，`CREATE TABLE IF NOT EXISTS` 不会改它）。
- 书按**文件内容指纹**（大小 + 首尾各 1MB 的 SHA1）识别，不按路径：用户会给文件改名、挪位置（发生过）。
- 分析结果连同 `ANALYSIS_VERSION` 和分析参数 `[max_angle, dpi]` 一起保存，重新打开时三者都对得上才复用，否则重新分析；用户的手动调整不受影响。**改了 `make_ink_mask` / `detect_skew` / `detect_content_bbox` 等检测逻辑，必须把 `ANALYSIS_VERSION` 加 1**，否则用户会继续看到旧算法的结果。对齐规则（`axis_shift` 等）不属于分析结果，改了不用动版本号。
- 打开文件时先 `reset_options()` 把「修正内容」和「参数」全部恢复成 `Options()` 的默认值，有保存记录的再 `restore_state` 覆盖上去：**每本书的设置互不沿用**（用户明确要求；早先的版本会沿用上一本的设置）。「辅助线」是显示偏好、不属于某本书，不重置也不保存。
- 启动时 `Store.prune_missing()` 删掉原文件已经不存在的书的记录（用户要求），日志里逐条写明。**只有「所在的盘还在、文件却没了」才删**：盘符或网络路径的根本身访问不到（移动硬盘没插、同步盘没挂载）只是暂时够不着，不能因此把校正进度全删掉。注意这和「按文件指纹识别、改名挪位置也认得」有取舍：文件被挪走后如果先启动了程序，记录就没了，再打开它只能重新来过。
- 保存是随改随存的：`request_preview`（翻页、改选项都会经过）、切换「不纠偏居中」、开始处理、关闭窗口时都会写库。数据库只在主线程访问。
- 「开始处理」上面有一个分析按钮：没分析过叫「分析全书」，有分析结果（现算的或从数据库恢复的）叫「重新分析」（`update_analyze_button`）：清掉 infos 重新跑一遍分析并覆盖保存，手动标记和各项设置保留。
- 数据库打不开时只弹一次警告，`self.store = None`，整形功能照常可用——所有用到 store 的地方都要判空。

### 界面线程模型
- fitz 文档**不跨线程共享**：每个线程自己 `fitz.open`。
- 同时只有一个工作线程（分析或处理）；预览另起短线程，用 `preview_gen` 序号丢弃过期结果。
- 线程只往 `self.msgs` 队列放消息，界面用 `after(50)` 轮询，线程里不碰任何 tk 对象。
- 用 `after()` 排队的延时任务（预览、大小预估）触发时，**要重新检查它依赖的状态还在不在**：排队期间用户可能点了「重新分析」或换了一本书，`self.infos` 已经是 None（大小预估曾因此报错）。
- **分析、处理期间锁住所有会改变输出的控件**（`set_locked`）：`start_analysis` 和点「开始处理」时锁上，完成/取消/出错时（`_idle`）恢复。处理用的是点击那一刻的设置，期间再改，界面上看着生效了、输出里却没有；版心微调和去污是挂在 `ref` 上的，处理线程一页一页地读，期间改了会前后不一致——所以 `start_process` 交给线程的是 `ref` 和逐页设定的**副本**。分析期间也锁是用户明确要求的（最初的方案是分析期间不锁、让用户趁这段时间设置选项，用户试用后否决了）。「取消」、翻页和预览、放大镜、辅助线不锁（只读，边等边翻看是有用的）；大小预估在处理期间停掉。平时一直可用的控件登记到 `self.lockable`，新加的设置项也要登记；各有各的可用条件的（逐页调整那一排）在自己的更新函数里判 `self.locked`。界面测试里的书很小，分析、处理一两秒就完：检查「期间仍然锁着」要防着它已经结束（`lock` 场景因此不稳定过）。
- **打开文件不自动分析**（用户要求，早先是打开即分析）：打开后先选书的类型、设好参数，点「分析全书」才分析；直接点「开始处理」也行，会先分析再处理。有保存的分析结果的书打开即恢复、不用分析。**分析之前所有逐页的按钮都不可用**（含「本页不纠偏居中」「删除当前页」——曾经这两个分析前就能点、第二排却不能，用户觉得怪），只有「分析全书」和「开始处理」能点。分析期间「开始处理」不可用；分析参数改过之后点「开始处理」会先重新分析，任务记入 `pending_run`，分析完自动接着跑。`analysis_key = (文件, 检测范围, DPI)`，这三者变了才需要重新分析，其余选项只影响 `plan_page`，改了只需刷新预览。

## 已知局限
- 输出是纯图像页：原 PDF 若带 OCR 文字层，被修正的页会丢失文字层（需要的话处理后用 `ocrmypdf` 重新识别）。目录书签和元数据会保留。
- 原扫描就把文字切到边缘的页无法复原，只会按可见内容居中。
- `--clean-margin` 只对黑白二值页有效；灰度/彩色页版心外的黑边要逐页用「去除边缘污染」。对文字切边的页效果未专门验证。

## 约定
- 用户用中文（简体）交流；代码注释、界面文字、命令行输出都用简体中文。
- 注释写"为什么"，尤其是踩过的坑；保持现有的注释密度和风格。
- 标准输出做了 `reconfigure(errors="replace")`，因为用户的 Windows 控制台可能不是 UTF-8 代码页。
